{% block title %}
    {{ echo_block("page_title") }}
        <header>
            <h1>{{ title }}</h1>
        </header>
    {{ echo_endblock() }}
{% endblock %}

{% block site_title %}
    {{ echo_block("title") }}
        {{ echo_twig('parent()') }} - {{ title }}
    {{ echo_endblock() }}
{% endblock %}
