{% extends '../CommonAdmin/NewAction/NewBuilderAction.php.twig' %}

{% block saveObject -%}
    protected function saveObject(\{{ model }} ${{ builder.ModelClass }})
    {
        $em = $this->getDocumentManager();
        $em->persist(${{ builder.ModelClass }});
        $em->flush();
    }
{% endblock %}
