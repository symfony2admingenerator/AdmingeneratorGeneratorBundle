{% import "AdmingeneratorGeneratorBundle:Form/EntityPicker:macros.html.twig" as macros %}

{%- for choice in choices -%}
    {%- if loop.first %}{{ '[' }}{% endif -%}
        {{ macros.datasource(choice.data, builder) }}
    {%- if not loop.last %}{{ ',' }}{% else %}{{ ']' }}{% endif -%}
{%- endfor -%}